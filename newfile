package com.schwab.brokerage.eventhub.kafka.logger;

import com.schwab.brokerage.eventhub.kafka.config.LogMessageConfiguration;
import com.schwab.kafka.logging.KafkaELILogRecord;
import com.schwab.kafka.logging.KafkaLogUtil;
import org.apache.kafka.clients.producer.RecordMetadata;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;
import schwab.discovery.client.ServiceId;

import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.mockito.Mockito.when;

class LogRecordGeneratorImplTest {

    @Mock
    private LogMessageConfiguration logMsgConfig;
    @Mock
    private KafkaLogUtil kafkaLogUtil;
    @Mock
    private RecordMetadata recordMetadata;
    @Mock
    private ServiceId serviceId;

    private LogRecordGeneratorImpl logRecordGenerator;

    @BeforeEach
    void setUp() {
        MockitoAnnotations.openMocks(this);
        logRecordGenerator = new LogRecordGeneratorImpl(logMsgConfig, kafkaLogUtil);
    }

    @Test
    void create_ShouldReturnLogRecord_ForSimpleMessage() {
        String msg = "Test message";
        ServiceCallType callType = ServiceCallType.MESSAGING;
        String operation = "Test operation";

        LogRecord logRecord = logRecordGenerator.create(msg, callType, operation);

        assertNotNull(logRecord);
        // Additional assertions based on the expected LogRecord content
    }

    @Test
    void create_ShouldReturnLogRecord_ForExceptionMessage() {
        String msg = "Test message";
        ServiceCallType callType = ServiceCallType.MESSAGING;
        String operation = "Test operation";
        Exception exception = new Exception("Test exception");

        LogRecord logRecord = logRecordGenerator.create(msg, callType, operation, exception);

        assertNotNull(logRecord);
        // Additional assertions based on the expected LogRecord content
    }

    @Test
    void create_ShouldReturnLogRecord_ForKafkaMessage() {
        String summary = "Test summary";
        String producerRecord = "Test producer record";
        when(recordMetadata.topic()).thenReturn("Test topic");

        LogRecord logRecord = logRecordGenerator.create(summary, recordMetadata, producerRecord, serviceId);

        assertNotNull(logRecord);
        // Additional assertions based on the expected LogRecord content
    }

    @Test
    void create_ShouldReturnLogRecord_ForKafkaException() {
        String summary = "Test summary";
        String topicName = "Test topic";
        Exception exception = new Exception("Test exception");

        LogRecord logRecord = logRecordGenerator.create(summary, topicName, exception, serviceId);

        assertNotNull(logRecord);
        // Additional assertions based on the expected LogRecord content
    }

    // Additional test cases...
}