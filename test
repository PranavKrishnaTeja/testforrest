@Test
public void getRabbitListenerEndpoint_ShouldReturnEndpoint_WhenEndpointIsNotNull() {
    ServiceId serviceId = new ServiceId(properties.getListenerServiceKey(),
                                        properties.getServiceInterface(),
                                        properties.getListenerServicePort());
    Endpoint expectedEndpoint = new Endpoint(); // Assuming Endpoint is a class you have
    when(arcataClient.getEndpoint(serviceId, QoS.MessageConsumer)).thenReturn(expectedEndpoint);

    Endpoint actualEndpoint = serviceRegistryLocator.getRabbitListenerEndpoint();

    assertEquals(expectedEndpoint, actualEndpoint);
}